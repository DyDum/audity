<ReglesCIS>
  <Rule id="4.1">
    <BasePath>$CATALINA_HOME/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the $CATALINA_HOME directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the $CATALINA_HOME directory. Command: chmod -R 750 $CATALINA_HOME; chown -R tomcat:tomcat $CATALINA_HOME;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME; chown -R tomcat:tomcat $CATALINA_HOME;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.2">
    <BasePath>$CATALINA_BASE/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the $CATALINA_BASE directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the $CATALINA_BASE directory. Command: chmod -R 750 $CATALINA_BASE; chown -R tomcat:tomcat $CATALINA_BASE;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_BASE; chown -R tomcat:tomcat $CATALINA_BASE;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_BASE</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.3">
    <BasePath>$CATALINA_HOME/conf/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the Tomcat configuration directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the Tomcat configuration directory. Command: chmod -R 750 $CATALINA_HOME/conf; chown -R tomcat:tomcat $CATALINA_HOME/conf;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME/conf; chown -R tomcat:tomcat $CATALINA_HOME/conf;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME/conf</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.4">
    <BasePath>$CATALINA_HOME/logs/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the Tomcat logs directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the Tomcat logs directory. Command: chmod -R 750 $CATALINA_HOME/logs; chown -R tomcat:tomcat $CATALINA_HOME/logs;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME/logs; chown -R tomcat:tomcat $CATALINA_HOME/logs;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME/logs</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.5">
    <BasePath>$CATALINA_HOME/temp/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the Tomcat temp directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the Tomcat temp directory. Command: chmod -R 750 $CATALINA_HOME/temp; chown -R tomcat:tomcat $CATALINA_HOME/temp;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME/temp; chown -R tomcat:tomcat $CATALINA_HOME/temp;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME/temp</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.6">
    <BasePath>$CATALINA_HOME/bin/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the Tomcat binaries directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the Tomcat binaries directory. Command: chmod -R 750 $CATALINA_HOME/bin; chown -R tomcat:tomcat $CATALINA_HOME/bin;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME/bin; chown -R tomcat:tomcat $CATALINA_HOME/bin;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME/bin</Verification>
    <Manual>NO</Manual>
  </Rule>

  <Rule id="4.7">
    <BasePath>$CATALINA_HOME/webapps/</BasePath>
    <FileName>N/A</FileName>
    <NonCompliantComment>Access to the Tomcat web application directory is not restricted, which may allow unauthorized modifications.</NonCompliantComment>
    <CorrectiveComment>Restrict access to the Tomcat web application directory. Command: chmod -R 750 $CATALINA_HOME/webapps; chown -R tomcat:tomcat $CATALINA_HOME/webapps;</CorrectiveComment>
    <Correction>chmod -R 750 $CATALINA_HOME/webapps; chown -R tomcat:tomcat $CATALINA_HOME/webapps;</Correction>
    <Verification>stat -c "%A %U %G" $CATALINA_HOME/webapps</Verification>
    <Manual>NO</Manual>
  </Rule>
</ReglesCIS>
